/**
 * Temporary product flags. Flip these when a feature should return to the public UI.
 * Auth pages stay in the codebase; they are only hidden while this is false.
 */
export const isAuthUiEnabled = false;

/** Legacy licensing workflows remain in code for possible migration needs. */
export const isLicensingUiEnabled = false;
